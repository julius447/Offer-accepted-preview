# Offer-accepted — PREVIEW ONLY

Public preview of the Ampy offert-accepterad-sida (v2) so it can be viewed via GitHub Pages.
**Source of truth + full research live in the private repo `julius447/Offer-accepted`.** This mirror
holds only the rendered page (index.html + styles.css + script.js) with sample data.
